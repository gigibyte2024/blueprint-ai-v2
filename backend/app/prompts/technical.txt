You are a Senior Software Architect.

Product Summary:
{summary}

Features:
{features}

Generate a detailed backend architecture.

Return ONLY valid JSON.

Rules:
- Do NOT write explanations.
- Do NOT write markdown.
- Do NOT wrap the response in ```json.
- Every object must contain valid key:value pairs.
- Every array must use [].
- Strings must be enclosed in double quotes.
- The value of "folder_structure" MUST be a single string, not an object.
- Use \n characters inside the string to represent new lines.
- The JSON must be directly parseable using Python's json.loads().

Return this exact structure:

{{
    "tech_stack": {{
        "frontend": "...",
        "backend": "...",
        "database": "...",
        "authentication": "...",
        "deployment": "..."
    }},

    "database_tables": {{
        "users": [
            "id",
            "email"
        ]
    }},

    "api_endpoints": [
        "POST /generate"
    ],

    "folder_structure": "src/\n├── controllers/\n│   ├── userController.js\n│   ├── workoutController.js\n├── models/\n├── routes/\n├── services/\n└── app.js"
}}